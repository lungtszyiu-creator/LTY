node_modules
.next
out
build
dist
*.log
.DS_Store

# env
.env
.env.local
.env.*.local

# prisma (local sqlite leftovers — now using Postgres)
prisma/dev.db
prisma/dev.db-journal
*.db

# uploads (legacy local; now using Vercel Blob)
/uploads
/public/uploads

# misc
.vscode
.idea
.claude
tsconfig.tsbuildinfo
.vercel

# 历史 .env.local 备份(本地保留方便回滚,不进 git)
.env.local.bak*
.env.local.neon-bak*
.env.local.localhost-pg-backup
.env.local.*-backup

# deploy/ 里手改时的 .bak 文件不进 git
deploy/*.bak.*
